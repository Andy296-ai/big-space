<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Retention
    |--------------------------------------------------------------------------
    |
    | Через сколько дней записи аудит-лога удаляются командой
    | activity-log:prune. По умолчанию — null, то есть хранить бессрочно;
    | команда в этом случае ничего не делает. Задайте
    | ACTIVITY_LOG_RETENTION_DAYS в .env, если политике организации нужен
    | конкретный срок хранения.
    |
    */

    'retention_days' => env('ACTIVITY_LOG_RETENTION_DAYS'),

];
